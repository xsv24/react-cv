{
  "name": "cv",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase": "^9.8.1",
    "google-map-react": "^2.1.0",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-scripts": "5.0.1",
    "react-transition-group": "^4.4.2",
    "sass": "^1.52.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy:dry": "yarn build && firebase serve --project $REACT_APP_FIREBASE_PROJECT",
    "deploy:prod": "yarn build && firebase deploy --project $REACT_APP_FIREBASE_PROJECT"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "eslint-plugin-react-hooks": "^4.5.0"
  }
}
